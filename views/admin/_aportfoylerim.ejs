<%- contentFor('title') %>
Portföylerim

<%- contentFor('body') %>
<section id="icerik" class="buyuk">
	<h4 class="well">PORTFÖYLERİM <p class="pull-right"> <a href="_portfoyEkle"><i class="fa fa-fw fa-plus"></i> Yeni Portföy</a></p></h4>
    
    <table id="datatable" class="table table-striped table-bordered">
    	<thead>
        	<tr>
            	
            	<th><i class="fa fa-fw fa-picture-o"></i> RESİM</th>
            	
            	
            </tr>
        </thead>
        <tbody>
        	<% prtfy.forEach(function(item){ %>
        	<tr>
            	<td >
                <a class="btn" href="_portfoyguncelle?id=<%= item.id %>" data-toggle="popover" data-placement="bottom" data-title="Adres" data-content="<%= item.adres %>" data-trigger="hover" ><i class="fa fa-fw fa-puzzle-piece"></i> <b><%= item.baslik %></a></b>
                <p class="pull-right">
                	
                    <a class="text-info" title="Güncelle" href="_portfoyguncelle?id=<%= item.id %>"><i class="fa fa-fw fa-pencil"></i></a>
									<a class="btn-ajax-confirm text-danger" frm="portfoySil" data-id="<%= item.id %>" title="Sil" style="cursor:pointer;"><i class="fa fa-fw fa-eraser"></i></a>
									
                                    <a class="btn-ajax-confirm text-<%= item.onay ? 'danger' : 'success'%>" frm="portfoyOnayla" data-id="<%= item.id %>" title="<%= item.onay ? 'Onay Kaldır' : 'Onayla'%>" style="cursor:pointer;"><i class="fa fa-<%= item.onay ? 'circle-o' : 'check'%>"></i></a>
							
                </p>
                <hr />
               <img src="uploads/<%= item.resim %>" class="img-rounded img-responsive" /></td>
            	
            	
            	
            </tr>
            <% }); %>
        </tbody>
    </table>
    
</section>
